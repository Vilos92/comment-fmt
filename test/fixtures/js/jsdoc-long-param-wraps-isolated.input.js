/**
 * A short description.
 * @param foo - this is a genuinely long parameter description that will definitely need to wrap across multiple lines because it goes on and on
 * @returns something
 */
function f(foo) {}
