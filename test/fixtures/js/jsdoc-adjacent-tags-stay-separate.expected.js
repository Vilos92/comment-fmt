/**
 * @param a - the first parameter which has a description long enough that it alone would need to wrap
 * @param b - the second parameter which also has a description long enough that it alone would need to wrap
 */
function f(a, b) {}
