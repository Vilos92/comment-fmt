/**
 * A description that is long enough to definitely need wrapping across more than one single physical
 * line for absolutely sure this time.
 * eslint-disable-next-line no-explicit-any -- rationale that would also wrap if it were treated as prose here
 */
function f() {}
