/**
 * Config:
 * - alpha does the first genuinely long thing that needs quite a lot of words to describe fully and
 *   overflows the one hundred and ten column limit for sure
 * - beta is short
 */
function g() {}
