/**
 * Applies damage to every entity within the blast radius, cleaning up already-dead entities first.
 * @sideEffect Mutates the entity registry and schedules a network broadcast.
 */
function explode() {}
