---
const title = 'Home';
---
<style>
  /* a genuinely long style comment inside a template's own style tag that overflows the one hundred and ten column limit */
  .foo { color: red; }
</style>
<script>
  // a genuinely long script comment inside a template's own script tag that overflows the one hundred and ten column limit
  const x = 1;
</script>
<p>{title}</p>
